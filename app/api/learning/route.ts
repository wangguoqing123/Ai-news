import { readLearningList } from "../../../lib/repositories/learning";import{requireRequestContext}from"../../../lib/server/auth";import{getSupabaseAdmin}from"../../../lib/server/supabase-admin";
export async function GET(request:Request){try{const context=await requireRequestContext(request);if(context.mode==="demo")return Response.json({mode:"demo",items:[]});return Response.json({mode:"live",items:await readLearningList(getSupabaseAdmin(),context.workspaceId)});}catch(error){if(error instanceof Response)return error;return Response.json({error:error instanceof Error?error.message:"读取学习列表失败"},{status:500})}}
